services:
  web:
    build: .
    ports:
      - "7871:7871"
    depends_on:
      - redis
    environment:
      - NODE_ENV=${NODE_ENV:-production} # Default to 'production' if not set
      - PORT=7871
    env_file:
      - .env.production
  redis:
    image: "redis:latest"
